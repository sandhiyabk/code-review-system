# ui/components/__init__.py
"""
Optional UI components for the Code Review Assistant.

These components are importable and can be optionally used by
ui/app.py. They are designed as additive features — the existing
app works exactly the same without them.

Usage in app.py:
    from ui.components.github_input import render_github_input
    from ui.components.evaluation_display import render_evaluation
"""
